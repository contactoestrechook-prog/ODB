# Lo que NO tiene sentido subir en cada deploy (el tarball pasaba de 100 MB
# por los videos de los reels y el upload moría por timeout).
docs/
brand/
*.mp4
*.mov
*.xls
*.xlsx
*.psd
*.ai
apps/mobile/
# otro producto: no va en el deploy de ODB
apps/gastro-admin/
apps/gastro-api/
apps/gastro-web/

# Excepción: los videos del sitio SÍ tienen que subir (11/9/2026: el video del
# Wine Fest daba 404 en producción porque la regla *.mp4 de arriba lo dejaba
# afuera). Solo esta carpeta; los reels siguen excluidos.
!apps/web/public/video/
!apps/web/public/video/*.mp4
